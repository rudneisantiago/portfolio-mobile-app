import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  h1: {
    fontSize: 30,
    fontWeight: 700,
  },
  textBold: {
    fontWeight: "bold",
  },
  subtitle: {
    fontSize: 15,
    fontWeight: 600,
    fontStyle: "italic",
  },
});
